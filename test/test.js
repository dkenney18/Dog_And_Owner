var assert = require('assert');
describe('Math', function() {
  describe('#Add 2 + 2', function() {
    it('should return 4 when 2 + 2 are added together', function() {
      assert.equal(2 + 2, 4);
    });
  });
});